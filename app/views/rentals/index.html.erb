
  <h3>Hi <%= @user.name %>, check your purchases! </h3>
  <h6>You have <%= @rentals.count %> purchases:</h6>
<% @rentals.each do |rental| %>
  <h1><%= rental.camera.name %></h1>
  <h2>£ <%= rental.camera.price %></h2>
  <p>Date of rental: <%= rental.camera.created_at.strftime("%d/%m/%Y at %I:%M%p") %></p>
  <%= link_to(new_camera_rental_path(rental), method: :delete) do %>
    <i class="fas fa-trash"></i>
  <% end %>
<% end %>

<%= link_to "List", cameras_path %>
