<!-- Navbar already rendered on this page from applications -->


<div class="btn-bar">
  <ul class="search-btn">
      <%= link_to 'Brand', cameras_path, class: "btn" %>
      <%= link_to 'Category', cameras_path, class: "btn" %>
      <%= link_to 'Price', cameras_path, class: "btn" %>
      <%= link_to 'Dates', cameras_path, class: "btn" %>
      <%= link_to 'Location', cameras_path, class: "btn" %>
  </ul>
</div>

<!-- iteration for cards to be created AKA keep creating cards if there's data-->


<div class="holder">
<!--   Add render here for camera cards and delete html below
 -->  <div class="cards">
    <% @many_cameras.each do |camera| %>
      <div class="cam-card">
        <%= cl_image_tag camera.photo, class: 'cam-img' %>
        <div class="info-holder">
          <p id="name-styling"><%= link_to camera.name, camera_path(camera) %>/<%= camera.brand %> </p>
          <p id="price-styling"> £<%= camera.price  %> </p>
          <p id="address-styling"> <%= camera.address %> </p>
        </div>
      </div>
    <% end %>

  </div>
  <div class="map-holder">
    <div
      id="map"
      style="width: 100%;
      height: 100%;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
    ></div>
  </div>
</div>

<!-- <form action="">
  <label for=""></label>
  <textarea name="" id="" cols="30" rows="10"></textarea>
  <select name="" id=""></select>
</form> -->

